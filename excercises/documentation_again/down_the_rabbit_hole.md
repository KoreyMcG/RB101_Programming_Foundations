Find the documentation for ```YAML::load_file```.

```ruby
require 'yaml'
MESSAGES = YAML.load_file('calculator_messages.yml')
```

> since yaml has to be 'required' it was a good indicator that it is in the
> ruby standard library. 
>
> Once heading to the 'yaml' page it notes it is an alias of the Psych module.
>
> https://ruby-doc.org/stdlib-3.1.1/libdoc/psych/rdoc/index.html
